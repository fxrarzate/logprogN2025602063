#include <stdio.h>
#include <math.h>
 int main ()
 {
     int L;
     L=2;
     float a=L*L;
     float b= L*L/2;
     float c= L*L/2;
     float d= ((3.0/2*L)*2.0)/2.0;
     float e=(2*L)*(1.0/4*2);
     float AT=a+b+c+d+e;

      float f=L*4
      float g=2*L+(sqrt(8)*2);
      float h=(3.0/2*L)+L+sqrt(5);
      float i=2*(1.0/4.0*L)+2*pow(L,2);

     float PT= f+g+h+i;


     printf("el perimetro total es:%f\n",PT);
     printf("el area total es:%f",AT );
     return 0;

 }
